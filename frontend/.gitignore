/node_modules
/.next
/out
/build
.DS_Store
*.pem
npm-debug.log*
# Ignore every .env variant (incl. .env.local, .env.local.bak, .env.production…)
# but keep the committed template.
.env*
!.env.example
*.bak
next-env.d.ts
